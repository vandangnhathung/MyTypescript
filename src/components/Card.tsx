import React, { useEffect } from "react";
import { ICard } from "../HungUtils/interfaces";

const Card = ({ title, description, image, link }: ICard) => {
  return <div></div>;
};

export default Card;
